# Leon Web Intel — runtime defaults (SQL is fixed in leon.py build_query()).

default_output: market_pulse.json
web_output: web/market_pulse.json
job_timeout_ms: 60000
maximum_bytes_billed: 500000000
