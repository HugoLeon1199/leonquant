GEMINI_API_KEY=your-gemini-api-key
# Digest batch: gemini-3.1-flash-lite (~100k input/request, free tier ~125k TPM).
GEMINI_MODEL=gemini-3.1-flash-lite

# GDELT BigQuery (leon.py — tab "Tin cập nhật từng phút", separate from 48h digest)
# Copy service-account JSON → credentials.json (already in .gitignore, never commit)
GOOGLE_APPLICATION_CREDENTIALS=credentials.json
GOOGLE_CLOUD_PROJECT=gdeltnews-497106
LEON_PULSE_OUTPUT=market_pulse.json
